# Components

Status: AWAITING FRAGMENT — no component code has been implemented yet.

Planned component library (see `/docs/04_COMPONENTS.md`):
- SiteHeader
- SiteFooter
- Breadcrumbs
- SearchBar
- Timeline
- CareerCard
- ProjectCard
- PublicationCard
- SpeakingCard
- DownloadCard

Rules: semantic HTML, keyboard accessible, reusable, data-driven.

Per the Claude Build Contract, each component renders from `/data` JSON records
rather than hardcoded page content. Schema card to be added when each
component is implemented.
